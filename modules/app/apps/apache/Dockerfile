# Dockerfile for a simple Nginx web server

# Use the official Nginx image from the Docker Hub
FROM httpd:latest

# Expose port 80 to the outside world 
EXPOSE 80

# No CMD needed - httpd image already starts Apache automatically

